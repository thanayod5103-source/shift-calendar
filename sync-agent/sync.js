// Deprecated: cloud Microsoft Graph sync is intentionally disabled.
// Use the local Windows agent in sync-agent/local-agent/ instead.
console.error('Microsoft Graph sync is disabled. Run the Local Excel Sync Agent on a PC that has access to the synced workbook.');
process.exit(1);
